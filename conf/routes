# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Registration

GET 	/processRegistration	controllers.Application.processRegistration()
GET     /registration	 	    controllers.Application.register()
GET 	/processLogin		    controllers.Application.processLogin()
GET     /map                    controllers.Application.map()
GET     /processHowFeeling      controllers.Application.processHowFeeling()
GET     /howFeeling             controllers.Application.howFeeling()